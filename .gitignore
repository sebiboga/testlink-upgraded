# IDE
.idea/
!.idea/tlcode.iml

# Environment / secrets
.env
.env.*
**/.env

# Database config (generated by install/Docker, never commit)
config_db.inc.php
config_db.inc.*
custom_config.inc.php
custom_config.inc.php.*

# OAuth / third-party credentials (contains real secrets)
custom_config.inc.php.example.*

# CI/opencode artifacts (temp files between workflow steps)
opencode.json
.ci_deadline_epoch
.ci_branch
.ci_before_sha
.ci_target_issue
tmp/wiki-repo

# Logs
logs/

# Uploads (user-generated content)
upload_area/**

# macOS
**/._*

# Node
node_modules/

# Composer binary (install globally or use Docker's)
composer.phar

# PHP local dev config (Windows-specific, not portable)
php.ini

# Heavy / scratch directories
tl200/
tmp/
vendor/

# Docker
.squash.yml

# Local GitHub OAuth credentials (contains real secrets, NEVER commit)
cfg/oauth_github_local.inc.php
