# SCREEN COMPARE STATUS (legacy vs modern feature-parity)

| Screen | Legacy controller | Modern screen | BFF | Status | Issues filed | Verdict |
|---|---|---|---|---|---|---|
| Test Plan with Custom Fields | `lib/results/testPlanWithCF.php` + `testPlanWithCF.tpl` | `gui/templates/results/tplanWithCF.html` | `api/reports` `tplan_with_cf` | ✓ | #847 #848 #858 #859 #860 #864 #865 #866 | 8 gaps filed total: #847 (suite grouping + collapsible group toolbar, OPEN) #848 (direct edit dropped → read-only viewer, FIXED) #858 (info_testPlanWithCF text missing, FIXED) #859 (generated-by-timestamp missing, FIXED) #860 (doubled glue-char in external ID, FIXED) + re-analysis 2026-09-04 ×2: #864 (per-column filters + Reset Filters action) #865 (custom-field columns not sortable) #866 (multi-column sort / drag headers to toolbar). Re-analysis 2026-09-04 run 2 confirmed the 4 open gaps (#847 #864 #865 #866) remain valid — re-annotated, no new distinct gaps filed. Re-analysis 2026-09-04 run 3 (browser-verified, fresh TPWCF fixtures: proj 39, plans 57/58/59): all 4 open task gaps (#847 #864 #865 #866) confirmed still present and re-annotated; NO new gaps filed. Newly documented (no issue filed): (a) legacy rows whose LAST custom-field column is empty are dropped by a `$hasValue = $cf_value ? true : false` OVERWRITE bug (testPlanWithCF.php:144-147) — modern BFF uses OR-logic and shows them (strict superset; intentional fix, e.g. legacy hides 'Report Gen' Owner=Alice/Tier='', modern shows it); (b) legacy 0-TC-plan path FATALS (count(null) at testPlanWithCF.php:111, PHP8 TypeError → HTTP 500) while modern shows the localized no_linked_tplan_cf empty state — modern strictly better; (c) 'Reset to Default State' + 'Refresh' toolbar buttons consolidated under #847 (noted in its first comment). Confirmed covered (browser-verified): rights (testplan_metrics enforced — BFF 403 for no-rights user verified, legacy redirects), CF header columns (Owner/Tier) + values, suite path, external ID + TC name, edit icon → tcEdit.html popup, non-empty-CF row filter, empty-state paths (all-empty CF values: legacy empty grid vs modern emptyBox), footer info text + generated-on + elapsed timestamp, sortable suite/test-case columns, pageLength 25/pagination/global search, i18n keys in all 10 bundles. |
| Event Viewer | `lib/events/eventviewer.legacy.php` + `eventviewer.tpl` + `eventinfo.php` + `eventinfo.tpl` | `gui/templates/eventviewer/eventviewer.html` | `api/eventviewer/index.php` | ✓ | #867 #868 #869 #870 #871 #872 #873 #874 | 8 gaps filed (all task, all OPEN, 2026-09-04): #867 (mgt_view_events view-right check missing on ALL GET routes — any auth user reads full log) #868 (multi-user filter dropped — legacy testers[] multi-select, modern single) #869 (log-level grouping + collapsible-group toolbar + Show All Columns / hidden Transaction column dropped) #870 (Clear Events not audited — legacy logs audit_all/with_level events_deleted) #871 (filters not reset after Clear Events — BUGID 3908) #872 (event detail drops level/description/user + real session_id; modern mislabels transaction id as "Session ID") #873 (empty state is DataTables hardcoded English "No data available in table", legacy localized no_events warning) #874 (Timestamp column string-sorts dd/mm/yyyy lexicographically, not chronological like legacy epoch-comment sort). Confirmed covered: level/date/user filters, Apply, object_id/object_type drill-down pill, Clear button gated on events_mgt, DELETE requires events_mgt, pie/line charts + legend, footer count + generated-on, i18n keys complete in all 10 bundles, detail expand fetch per event, pagination/pageLength 25, default timestamp desc intent. Browser-verified with 21+ seeded events + no-rights user (vguest). |
| User Profile | `lib/usermanagement/userInfo.php` + `userInfo.tpl` | `gui/templates/usermanagement/userInfo.html` | `api/userinfo/index.php` | ✓ | #875 #876 #877 #878 | 4 gaps filed (all task, all OPEN, 2026-09-04): #875 (external password-management gating — legacy hides change-password form when `isPasswordMgtExternal()`, modern always shows + BFF always allows) #876 (API-enabled gating — legacy wraps API-key section in `$tlCfg->api->enabled`, modern always shows + always allows keygen) #877 (Show event history link gated on `mgt_view_events` right — legacy `showEventHistoryFor` drill-down to Event Viewer, modern has none) #878 (demoMode read-only — legacy disables Save/Change-Password + shows `demo_update_user_disabled`, modern always editable). Confirmed covered (browser-verified): edit personal data (first/last name, email, locale) with client+server required/format validation, change password with old-password compare + new/confirm match, API key display + generate + copy, successful (10) + failed (10) login history with localized empty states, role + login readonly display, session/title-bar refresh on save. Concluded: only the 4 config/rights-gated features above are genuinely missing — all core editing/history UI is at parity. |
| User Management | `lib/usermanagement/usersView.php` + `usersView.tpl` (+ `usersEdit.php`/`usersEdit.tpl`, `usersExport.php`/`usersExport.tpl`) | `gui/templates/usermanagement/usersView.html` | `api/users/index.php` | ✓ | #879 #880 #881 #882 #883 #884 #885 #886 #887 #888 #889 #890 #891 | 13 gaps filed (all task, all OPEN, 2026-09-04): #879 (mgt_users rights check missing on ALL BFF routes — any auth user lists & creates users; guest created an admin-role account, privilege escalation) #880 (Export to users.xml dropped) #881 ('Manage user' search-by-login box dropped) #882 (Authentication method + Expiration Date fields missing from create/edit modal) #883 (Locale + Expiration Date columns missing from grid) #884 (Reset Password action missing) #885 (Generate API Key action missing) #886 (Show-event-history link gated on mgt_view_events missing) #887 (demoMode read-only gating missing) #888 (create allows empty password — inserts user with password='' vs legacy requiring it) #889 (role dropdown includes <inherited> id=0 + create defaults to reserved role id=1 instead of default_roleid guest) #890 (no success/failure user-feedback messages after create/disable/update) #891 (grid toolbar Expand/Collapse Groups, Show all Columns, Reset to Default State, Refresh, Reset Filters dropped). Confirmed covered (browser-verified with user1/nopass/vguest fixtures): create user modal, edit user modal (login readonly, fn/ln/email/locale/role/active), enable/disable (+ cannot-disable-yourself guard), delete (soft-delete active=2, admin protection), DataTables sort/search/pagination, i18n via TLi18n, tabs to Role/Assign screens, generated-on footer. |
