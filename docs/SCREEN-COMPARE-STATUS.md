# SCREEN COMPARE STATUS (legacy vs modern feature-parity)

| Screen | Legacy controller | Modern screen | BFF | Status | Issues filed | Verdict |
|---|---|---|---|---|---|---|
| Test Plan with Custom Fields | `lib/results/testPlanWithCF.php` + `testPlanWithCF.tpl` | `gui/templates/results/tplanWithCF.html` | `api/reports` `tplan_with_cf` | ✓ | #847 #848 #858 #859 #860 #864 #865 #866 | 8 gaps filed total: #847 (suite grouping + collapsible group toolbar, OPEN) #848 (direct edit dropped → read-only viewer, FIXED) #858 (info_testPlanWithCF text missing, FIXED) #859 (generated-by-timestamp missing, FIXED) #860 (doubled glue-char in external ID, FIXED) + re-analysis 2026-09-04 ×2: #864 (per-column filters + Reset Filters action) #865 (custom-field columns not sortable) #866 (multi-column sort / drag headers to toolbar). Re-analysis 2026-09-04 run 2 confirmed the 4 open gaps (#847 #864 #865 #866) remain valid — re-annotated, no new distinct gaps filed. Re-analysis 2026-09-04 run 3 (browser-verified, fresh TPWCF fixtures: proj 39, plans 57/58/59): all 4 open task gaps (#847 #864 #865 #866) confirmed still present and re-annotated; NO new gaps filed. Newly documented (no issue filed): (a) legacy rows whose LAST custom-field column is empty are dropped by a `$hasValue = $cf_value ? true : false` OVERWRITE bug (testPlanWithCF.php:144-147) — modern BFF uses OR-logic and shows them (strict superset; intentional fix, e.g. legacy hides 'Report Gen' Owner=Alice/Tier='', modern shows it); (b) legacy 0-TC-plan path FATALS (count(null) at testPlanWithCF.php:111, PHP8 TypeError → HTTP 500) while modern shows the localized no_linked_tplan_cf empty state — modern strictly better; (c) 'Reset to Default State' + 'Refresh' toolbar buttons consolidated under #847 (noted in its first comment). Confirmed covered (browser-verified): rights (testplan_metrics enforced — BFF 403 for no-rights user verified, legacy redirects), CF header columns (Owner/Tier) + values, suite path, external ID + TC name, edit icon → tcEdit.html popup, non-empty-CF row filter, empty-state paths (all-empty CF values: legacy empty grid vs modern emptyBox), footer info text + generated-on + elapsed timestamp, sortable suite/test-case columns, pageLength 25/pagination/global search, i18n keys in all 10 bundles. Re-analysis 2026-09-04 run 4 (browser-verified, fresh fixtures proj 200 'TPWCF Proj' / plan 209 'TPWCF Plan A', CF Owner/Tier): all 4 open task gaps (#847 #864 #865 #866) confirmed still present and re-annotated (comment on each); NO new distinct gaps filed. Live legacy re-confirmed the ExtTable toolbar (Expand/Collapse Groups, Show all Columns, Reset to Default State, Refresh, Reset Filters, MultiSort) + group-by-suite-with-(N Item); modern DataTable lacks all. Re-confirmed covered: edit icon→tcEdit.html popup, external ID single glue char (TPWC-1), suite path, CF values (modern shows TC Alpha Owner=Alice/Tier='' that legacy drops via overwrite bug), empty-plan 210 (legacy HTTP 500 TypeError, modern localized no_linked_tplan_cf empty state), footer info+generated-on+elapsed, DataTable sort/search/pagination, tpwcf.* keys + valid JSON in all 10 bundles. |
| Event Viewer | `lib/events/eventviewer.legacy.php` + `eventviewer.tpl` + `eventinfo.php` + `eventinfo.tpl` | `gui/templates/eventviewer/eventviewer.html` | `api/eventviewer/index.php` | ✓ | #867 #868 #869 #870 #871 #872 #873 #874 | 8 gaps filed (all task, all OPEN, 2026-09-04): #867 (mgt_view_events view-right check missing on ALL GET routes — any auth user reads full log) #868 (multi-user filter dropped — legacy testers[] multi-select, modern single) #869 (log-level grouping + collapsible-group toolbar + Show All Columns / hidden Transaction column dropped) #870 (Clear Events not audited — legacy logs audit_all/with_level events_deleted) #871 (filters not reset after Clear Events — BUGID 3908) #872 (event detail drops level/description/user + real session_id; modern mislabels transaction id as "Session ID") #873 (empty state is DataTables hardcoded English "No data available in table", legacy localized no_events warning) #874 (Timestamp column string-sorts dd/mm/yyyy lexicographically, not chronological like legacy epoch-comment sort). Confirmed covered: level/date/user filters, Apply, object_id/object_type drill-down pill, Clear button gated on events_mgt, DELETE requires events_mgt, pie/line charts + legend, footer count + generated-on, i18n keys complete in all 10 bundles, detail expand fetch per event, pagination/pageLength 25, default timestamp desc intent. Browser-verified with 21+ seeded events + no-rights user (vguest). |
| User Profile | `lib/usermanagement/userInfo.php` + `userInfo.tpl` | `gui/templates/usermanagement/userInfo.html` | `api/userinfo/index.php` | ✓ | #875 #876 #877 #878 | 4 gaps filed (all task, all OPEN, 2026-09-04): #875 (external password-management gating — legacy hides change-password form when `isPasswordMgtExternal()`, modern always shows + BFF always allows) #876 (API-enabled gating — legacy wraps API-key section in `$tlCfg->api->enabled`, modern always shows + always allows keygen) #877 (Show event history link gated on `mgt_view_events` right — legacy `showEventHistoryFor` drill-down to Event Viewer, modern has none) #878 (demoMode read-only — legacy disables Save/Change-Password + shows `demo_update_user_disabled`, modern always editable). Confirmed covered (browser-verified): edit personal data (first/last name, email, locale) with client+server required/format validation, change password with old-password compare + new/confirm match, API key display + generate + copy, successful (10) + failed (10) login history with localized empty states, role + login readonly display, session/title-bar refresh on save. Concluded: only the 4 config/rights-gated features above are genuinely missing — all core editing/history UI is at parity. |
| User Management | `lib/usermanagement/usersView.php` + `usersView.tpl` (+ `usersEdit.php`/`usersEdit.tpl`, `usersExport.php`/`usersExport.tpl`) | `gui/templates/usermanagement/usersView.html` | `api/users/index.php` | ✓ | #879 #880 #881 #882 #883 #884 #885 #886 #887 #888 #889 #890 #891 | 13 gaps filed (all task, all OPEN, 2026-09-04): #879 (mgt_users rights check missing on ALL BFF routes — any auth user lists & creates users; guest created an admin-role account, privilege escalation) #880 (Export to users.xml dropped) #881 ('Manage user' search-by-login box dropped) #882 (Authentication method + Expiration Date fields missing from create/edit modal) #883 (Locale + Expiration Date columns missing from grid) #884 (Reset Password action missing) #885 (Generate API Key action missing) #886 (Show-event-history link gated on mgt_view_events missing) #887 (demoMode read-only gating missing) #888 (create allows empty password — inserts user with password='' vs legacy requiring it) #889 (role dropdown includes <inherited> id=0 + create defaults to reserved role id=1 instead of default_roleid guest) #890 (no success/failure user-feedback messages after create/disable/update) #891 (grid toolbar Expand/Collapse Groups, Show all Columns, Reset to Default State, Refresh, Reset Filters dropped). Confirmed covered (browser-verified with user1/nopass/vguest fixtures): create user modal, edit user modal (login readonly, fn/ln/email/locale/role/active), enable/disable (+ cannot-disable-yourself guard), delete (soft-delete active=2, admin protection), DataTables sort/search/pagination, i18n via TLi18n, tabs to Role/Assign screens, generated-on footer. |
| Role Management | `lib/usermanagement/rolesView.php` + `rolesView.tpl` (+ `rolesEdit.php`/`rolesEdit.tpl`) | `gui/templates/usermanagement/rolesView.html` | `api/roles/index.php` | ✓ | #897 #898 #899 #900 #901 #902 #903 #904 | 8 gaps filed (all task, all OPEN, 2026-09-04): #897 (role_management rights check missing on ALL BFF routes — any auth user lists/creates/edits/deletes roles; guest session browser-verified loading the full screen) #898 (system-role protection broken — BFF hardcodes `systemRoleId=3` instead of `TL_LAST_SYSTEM_ROLE=9`, so roles 4-9 (guest/tester/admin/leader/…) are mislabeled Custom and get a working delete button; legacy makes system roles undeletable) #899 (legacy edit lock dropped — admin role id=8 `TL_ROLES_ADMIN` and `<no rights>` id=3 are read-only/unsavable in legacy, modern edit modal allows full edit + PUT for any role) #900 (Duplicate Role action missing — legacy per-row duplicate → `doAction=duplicate` copies role with generated unique name + audit CREATE; modern has edit/delete only) #901 (Show-event-history link missing — legacy `showEventHistoryFor(roleid,'roles')` info icon gated on `mgt_view_events`; modern modal has none) #902 (delete confirmation drops affected-users + reset warning — `/{id}/users` route shadowed by `/{id}` so `#deleteUsers` stays empty; legacy lists affected users + 'users will be reset to <replacement role>' before confirmDelete) #903 (demoMode read-only gating missing — legacy replaces Save with `demo_update_role_disabled` when `$tlCfg->demoMode` ON; modern always allows POST/PUT/DELETE) #904 (no success/failure user-feedback after create/update/delete — legacy `inc_update.tpl result=$sqlResult`/`user_feedback=` shows localized result; modern silently reloads on success, ad-hoc English errors). Confirmed covered (browser-verified admin + guest sessions): role grid (ID/Name/Description/Rights/Type/Actions), DataTables sort/search/pagination/pageLength, System/Custom badge (broken for 4-9 per #898), create modal (name + description + all-rights checkboxes + Select All + at-least-one-right & non-empty-name validation via `role.validation.*`), edit modal loads any role + rights, delete confirm modal (broken users list per #902), SYSTEM-role deletion guard only for ids ≤3 (wrong vs legacy), tabs to User Management / Assign Project+Plan roles, i18n keys present in all bundles (role.*), footer roles-count + generated-on. |
| Test Specification | `lib/testcases/listTestCases.php?feature=edit_tc` + `tcTree.tpl` (+ `archiveData.php`/`tcView.tpl`, `tcEdit.php`, `testcaseCommands.class.php`) | `gui/templates/testcases/testSpec.html` | `api/testcases/index.php` | ✓ | #906 #907 #908 #909 #910 #911 #912 #913 #914 #915 #916 #917 #918 #919 | 12 gaps filed (all task, all OPEN, 2026-09-04): #906 (full step management — insert/copy/reorder/resequence rows + per-step execution type; modern editor only add-at-end/remove) #907 (design-time custom-field inputs + persistence in cfield_design_values; project CF "Tier" has no field in editor) #908 (testcase STATUS select dropped from editor; BFF create/update never write status) #909 (edit-mode filter panel — TC id/title/top-level suite/keyword Or/And/Not/platform/status/importance/execution-type/custom-field filters + Apply/Reset/HideCF + expand/collapse + refresh-on-action setting; modern tree only name filter) #910 (drag-and-drop move/copy of suites/testcases gated on mgt_modify_tc; modern tree read-only) #911 (estimated execution duration field with validation; only shown read-only in tcView.html) #912 (requirements assignment — openReqWindow / req_tcase_link_management; modern has no link UI) #913 (attachment upload/delete; modern tcView.html lists read-only only) #914 (version selection + lifecycle: single-version delete, freeze/unfreeze, activate/deactivate/setIsOpen; modern edits latest only and delete removes ALL versions) #915 (per-testcase-version platform assignment; testcase_platforms) #916 ('add to test plan' action — doAdd2testplan) #917 (duplicate test case name check/warning — legacy checkTCaseDuplicateName inline warning). Plus 2 bugs filed while testing: #918 (legacy tcView.tpl:105 Smarty syntax error from commit b07f0df40 alike isset()-guard — breaks the whole legacy View/Edit Test Case screen, archiveData.php?edit=testcase) #919 (modern editor keyword picker renders PHP tlKeyword object dumps + positional ids — BFF get keywordsProject serialization + testSpec.html formHtml). Confirmed covered (browser-verified, fresh DB fixtures proj 1 "Fixture Project Alpha", suites Login/Reports/Advanced, TCs 5/9/12/15, plan 20 build 1 + executions): tree load/count/expand (3 suites · 4 cases), name filter, detail view (ver/extid/importance/execution type/id/summary/preconditions/steps/keywords), edit modal fields present (name, importance, execution type, summary, preconditions, steps add/remove, keywords) with Save/Cancel, create new TC, create new version (tcversion_id 17), delete TC, Full Viewer link, mgt_view_tc write gating on BFF create/update/delete, executed-version 403 guard (testproject_edit_executed_testcases), i18n via TLi18n. Verdict: tree+core-editor at parity; advanced editor features, tree tooling and version/requirements/platform actions are the outstanding gaps above. |
| Assign Test Project Roles | `lib/usermanagement/usersAssign.php` (featureType=testproject) + `gui/templates/dashio/usermanagement/usersAssign.tpl` | `gui/templates/usermanagement/usersAssignProject.html` | `api/roles/index.php` (`meta/tproject-roles` + `tproject-roles`) | ✓ | #924 #925 #926 #927 #928 #929 #930 #931 #932 | 9 gaps filed (all task, all OPEN, 2026-09-04): #924 (access/read rights check missing — legacy `checkRights()` redirects no-rights users + audits `audit_security_user_right_missing` and the project combo only lists projects whose caller effective role has `user_role_assignment`/`testproject_user_role_assignment`; modern BFF GET returns 200 with full users+roles for any authenticated user incl. frank global `<no rights>`) #925 (update rights check missing — legacy `checkRightsForUpdate()` gates the write; modern PUT tproject-roles has none; frank browser-verified wrote role (2,1,7), privilege escalation) #926 (effective/inherited role display dropped — legacy shows `<inherited> <globalRoleName>` / `<no rights>` effective state; modern shows `-- no role --` + a corrupt empty `<inherited>` option, inherited name never revealed) #927 (global-admin rows editable — legacy disables the select + "Global Admin can not be changed" hint; modern lets you edit admin users' project role) #928 (`admin` role id 8 still offered in dropdown — legacy removes the option; modern renders it selectable) #929 ("Set roles to <role> + Do" bulk assignment action missing) #930 (DataTables pagination + "User" search + sort missing — legacy paginated 20/40/60/All table; modern static list) #931 (no localized success/failure feedback after save — legacy shows "User Roles updated" via inc_update; modern reloads silently) #932 (demoMode read-only gating missing — legacy blocks submit with warn_demo). Confirmed covered (browser-verified, fresh DB fixtures: projects #1 pub "Roles Pub Project" RP + #2 priv "Roles Priv Project" PV; users admin/alice(tester)/bob(leader)/carol(guest)/dave(admin)/erin(senior tester)/frank(no rights)): project selector + user table of all ACTIVE users (login + firstName lastName, active=1 filter matching legacy), per-user role select defaulting to the assigned role, save writes delete+addUserRole correctly (row 2,1,5 verified), audit trail preserved (per-user `audit_users_roles_added_testproject` ASSIGN events survive via testproject::addUserRole, legacy detail events match), tabs bar to User/Role/Assign-Plan screens, session-preserved tproject_id context, empty state when no project/selection, i18n assign.* keys via TLi18n. Verdict: core assignment read/write at parity for admins, but rights enforcement (read+write), inherited-role display and the bulk/table/user-feedback/demoMode features are the 9 outstanding gaps above. |
